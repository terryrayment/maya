<li class="grid-product">
  <a class="grid-product-link" href="{{ product.url }}">
    <div class="grid-product-image-container">
      {% if media %}
        {% if media.media_type == 'video' %}
          {{
            media
            | video_tag:
              playsinline: true,
              controls: false,
              autoplay: true,
              muted: true,
              loop: true,
              class: 'grid-product-image'
          }}
        {% else %}
          {% assign image = media %}
          <img
            class="grid-product-image"
            width="{{ image.width }}"
            height="{{ image.height }}"
            srcset="{{ image | image_url: width: 200 }} 200w, {{ image | image_url: width: 300 }} 300w, {{ image | image_url: width: 400 }} 400w, {{ image | image_url: width: 500 }} 500w, {{ image | image_url: width: 600 }} 600w, {{ image | image_url: width: 600 }} 600w, {{ image | image_url: width: 700 }} 700w, {{ image | image_url: width: 800 }} 800w, {{ image | image_url: width: 900 }} 900w, {{ image | image_url: width: 1000 }} 1000w, {{ image | image_url: width: 1200 }} 1200w, {{ image | image_url: width: 1400 }} 1400w, {{ image | image_url: width: 1800 }} 1800w, {{ image | image_url: width: 2200 }} 2200w"
            sizes="{{ image_sizes }}"
            src="{{ image | image_url: width: 600 }}"
            alt="{{ image.alt | escape }}"
            loading="lazy"
          >
        {% endif %}
      {% else %}

      {% endif %}
    </div>
    <div class="button grid-product-button">
      <svg class="button-border">
        <rect width="calc(100% - 2px)" height="calc(100% - 2px)" rx="5" />
      </svg>
      {% if product.metafields.custom.email_for_price %}
      {% elsif product.metafields.custom.preorder %}

      {% elsif product.available %}

      {% else %}
        {% assign is_sold_out = true %}
      {% endif %}
      <h3 class="grid-product-title">{{ product.title }}</h3>
      {% unless product.metafields.custom.email_for_price %}
        <p class="grid-product-price">
          {{ product.price | money_without_trailing_zeros }}
          {% if product.compare_at_price %}
            <del>
              {{ product.compare_at_price | money_without_trailing_zeros }}
            </del>
          {% endif %}
        </p>
      {% endunless %}
    </div>
  </a>
</li>
